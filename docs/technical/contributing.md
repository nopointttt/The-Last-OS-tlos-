# tLOS Contribution Guide (Руководство контрибьютора)

Добро пожаловать в разработку **The Last OS (tLOS)**. Мы строим суверенную пространственную операционную систему, и ваш вклад поможет сделать ее стандартом будущего.

## 🤝 Кодекс Чести (Code of Conduct)
Мы придерживаемся принципов взаимного уважения и инклюзивности. Любая форма агрессии или дискриминации недопустима. Подробности в [CODE_OF_CONDUCT.md](../CODE_OF_CONDUCT.md).

## 🚀 С чего начать?
1.  **Исследуйте Issues**: Ищите теги `good first issue` для легкого входа.
2.  **Обсудите изменения**: Для крупных фич обязательно создайте **RFC** (Request for Comments) по [шаблону](templates/rfc_template.md).
3.  **Синхронизируйтесь**: Мы используем Discord для оперативного общения и еженедельные Community Calls.

## 🛠 Технический стек и инструменты
Чтобы начать работу, вам понадобятся:
- **Rust Toolchain**: `rustup`, `clippy`, `rustfmt`.
- **wasmCloud Shell (`wash`)**: Основной инструмент для сборки компонентов и управления Латицей.
- **Node.js / SolidJS**: Для работы над Shell (Frontend).

## 🧪 Рабочий процесс (Workflow)
Мы используем стандартный GitHub Flow с дополнениями от CNCF:

1.  **Fork** репозитория и создание функциональной ветки (`feat/...`, `fix/...`).
2.  **DCO (Developer Certificate of Origin)**: Мы требуем подписи всех коммитов. Добавляйте `-s` при коммите:
    ```bash
    git commit -s -m "feat: добавлена поддержка пространственных метаданных"
    ```
3.  **Conventional Commits**: Мы используем этот формат для автоматизации чейнджлогов:
    - `feat:` — новая функциональность.
    - `fix:` — исправление багов.
    - `docs:` — изменения в документации.
    - `refactor:` — изменения кода без изменения логики.

## 📐 Стандарты кода
- **Rust**:
35:   - Код должен проходить проверку `cargo fmt` и `cargo clippy`.
36:   - Минимизируйте `clone()` и используйте идиоматичные абстракции (Iterators).
37:   - Актёры должны взаимодействовать через WIT-интерфейсы.
38:   - **Zero-Web2**: Использование HTTP, REST и других legacy-протоколов запрещено. См. [Zero-Web2 Standards](protocol/zero_web2_standards.md).
39: - **Frontend (SolidJS/WGPU)**:
  - Используйте компоненты из нашей Design System.
  - Весь UI должен быть доступным (A11y).
  - Пространственная математика должна следовать спецификации Lattice.

## 🛡 Безопасность и AI-резистентность
tLOS спроектирована с учетом атак со стороны AI-агентов. При разработке:
- Никогда не используйте ненадежные внешние библиотеки без аудита.
- Все сетевые сообщения должны быть подписаны Ed25519/Schnorr.
- При обнаружении уязвимостей сообщайте через **Security Policy** (не в публичных Issues).

## 📝 Документация: The Bible Protocol
В tLOS документация (`.docs/`) является **"Библией"** — единственным источником истины.
- **Deep Sync**: Каждый новый агент обязан прочесть ВСЕ файлы в `.docs/` и `.context/` перед началом работы (`/handshake`).
- **Patch Workflow**: Изменения в Библию вносятся только через аддитивные патчи, исключая галлюцинации.
- **Handover**: В конце сессии все архитектурные открытия должны быть зафиксированы в Библии (`/handover`).

### Типы документов:
- Новые архитектурные решения -> **ADR**.
- Новые интерфейсы -> **WIT + RFC**.
- Новые фичи -> **User Manual**.

---
*Ваш вклад — это кирпич в фундамент суверенного цифрового будущего. Спасибо, что вы с нами!*
